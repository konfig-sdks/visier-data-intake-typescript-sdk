import { VisierDataIntake } from "./index";

describe("visier-data-intake-typescript-sdk", () => {
    it("initialize client", async () => {
        const visierdataintake = new VisierDataIntake({
        });
    });
});
